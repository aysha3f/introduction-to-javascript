var price = 35;
// price = price + 10;
price += 12; //price = price + 10; both are same
console.log(price);

var fan =37000;
// fan = fan - 1000;
fan -= 1000; // fan = fan - 1000; both are same. it can only use when variable is same
console.log(fan);


